
import React from 'react';

const NoTimeSlotMessage: React.FC = () => {
  return (
    <div className="text-center py-6">
      <p className="text-gray-400">Please select a time slot first</p>
    </div>
  );
};

export default NoTimeSlotMessage;
